# temporal-version-exporter
Repository scaffold for a small Go exporter that queries a Temporal frontend (gRPC) for system/cluster info, extracts the server version, and exposes it as a Prometheus metric.
